class Comment {
    constructor(id, comment, createdAt) {
        this.id = id;
        this.comment = comment;
        this.createdAt = createdAt;
    }
}

module.exports = {
    Comment: Comment
};
